#!/bin/bash

usage() {
  echo "Usage: ${0##*/} <repository_name>" >&2
  exit 1
}

if (($# != 1)); then
  usage
fi

repository_name=$1

aws-ecr-list-all-images $repository_name \
  | jq '.imageIds | map(select(.imageTag)) | .[].imageTag' -r
