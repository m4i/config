if { read -r RUBY_AUTO_VERSION < .ruby-version; } 2>/dev/null; then
  if [[ -e /usr/local/share/chruby/chruby.sh ]]; then
    source /usr/local/share/chruby/chruby.sh
    chruby "$RUBY_AUTO_VERSION"
    export RUBY_AUTO_VERSION
  fi
fi
